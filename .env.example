# =============================================================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (prefixed with VITE_)
# These are baked into the frontend at build time
# =============================================================================

# API URL for static deployments (e.g., GitHub Pages, Netlify, Vercel)
# Set this to your Cloudflare Worker URL when deploying the frontend statically
# Format: https://your-worker.your-subdomain.workers.dev/api
# 
# For local development with Cloudflare plugin (combined mode), leave this unset
# to use relative URLs (/api)
# VITE_API_URL=https://your-worker.your-subdomain.workers.dev/api

# =============================================================================
# SERVER-SIDE ENVIRONMENT VARIABLES (Cloudflare Worker secrets)
# Set these using: bunx wrangler secret put NAME --config wrangler.worker.jsonc
# =============================================================================

# CORS: Comma-separated list of allowed origins
# Required when deploying frontend separately (e.g., GitHub Pages)
# Example: https://username.github.io,https://yourdomain.com
# ALLOWED_ORIGINS=

# Security: CSRF token secret (min 32 chars)
# CSRF_SECRET=

# Security: Session encryption secret (min 32 chars)  
# SESSION_SECRET=

# App environment
# NODE_ENV=production
